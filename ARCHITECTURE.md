# 📐 Password Manager - Architecture Diagrams

## 🏗️ System Architecture

### C++ Version Flow
```
┌─────────────────────────────────────────────────────────────┐
│                        USER STARTS PROGRAM                   │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
            ┌────────────────────────┐
            │   Login (Max 3 tries)  │
            │  - Masked Input (***) │
            │  - Hash Verification   │
            └────────┬───────────────┘
                     │
        ┌────────────┴────────────┐
        │ Success?               │
        └────┬──────────────┬────┘
             │ NO           │ YES
             ▼              ▼
        ┌─────────┐    ┌──────────────────────┐
        │  EXIT   │    │  Load passwords.txt  │
        └─────────┘    └──────────┬───────────┘
                                  │
                                  ▼
                    ┌─────────────────────────┐
                    │    MAIN MENU (1-9)      │
                    │  - Add Password         │
                    │  - View Passwords       │
                    │  - Decrypt Password     │
                    │  - Update Password      │
                    │  - Delete Password      │
                    │  - Generate Password    │
                    │  - Search Passwords     │
                    │  - Export Passwords     │
                    │  - Exit                 │
                    └────────┬────────────────┘
                             │
                             ▼
                    ┌────────────────────┐
                    │  Process Action    │
                    │  - Encrypt/Decrypt │
                    │  - Check Expiry    │
                    │  - Save to File    │
                    └────────┬───────────┘
                             │
                             ▼
                    ┌────────────────────┐
                    │  Back to Menu      │
                    └────────────────────┘
```

---

## 🌐 React Version Architecture

### Application Structure
```
┌──────────────────────────────────────────────────────────┐
│                         BROWSER                           │
│  ┌─────────────────────────────────────────────────┐    │
│  │                  React App                       │    │
│  │  ┌────────────────────────────────────────┐     │    │
│  │  │         React Router DOM              │     │    │
│  │  │  ┌──────────────────────────────┐     │     │    │
│  │  │  │        Routes                │     │     │    │
│  │  │  │  - / (Login)                 │     │     │    │
│  │  │  │  - /dashboard                │     │     │    │
│  │  │  │  - /passwords                │     │     │    │
│  │  │  │  - /add                      │     │     │    │
│  │  │  │  - /edit                     │     │     │    │
│  │  │  │  - /settings                 │     │     │    │
│  │  │  └────────────┬─────────────────┘     │     │    │
│  │  └───────────────┼───────────────────────┘     │    │
│  │                  │                             │    │
│  │  ┌───────────────▼───────────────────┐         │    │
│  │  │      Zustand Store                │         │    │
│  │  │  - passList                       │         │    │
│  │  │  - isAuthenticated                │         │    │
│  │  │  - searchQuery                    │         │    │
│  │  │  - selectedPass                   │         │    │
│  │  │  + addNewPass()                   │         │    │
│  │  │  + updatePass()                   │         │    │
│  │  │  + deletePass()                   │         │    │
│  │  │  + getDecryptedPass()             │         │    │
│  │  └───────────────┬───────────────────┘         │    │
│  │                  │                             │    │
│  │  ┌───────────────▼───────────────────┐         │    │
│  │  │      Utils Layer                  │         │    │
│  │  │  ┌─────────┐     ┌──────────┐    │         │    │
│  │  │  │crypto.js│     │storage.js│    │         │    │
│  │  │  │-encrypt │     │-save     │    │         │    │
│  │  │  │-decrypt │     │-load     │    │         │    │
│  │  │  │-strength│     │-export   │    │         │    │
│  │  │  └─────────┘     └──────────┘    │         │    │
│  │  └───────────────┬───────────────────┘         │    │
│  └──────────────────┼─────────────────────────────┘    │
│                     │                                   │
│     ┌───────────────▼──────────────┐                   │
│     │      LocalStorage            │                   │
│     │  - masterPassword            │                   │
│     │  - passwordList              │                   │
│     └──────────────────────────────┘                   │
└──────────────────────────────────────────────────────────┘
```

---

## 🔐 Encryption Flow

### Password Save Process
```
┌─────────────┐
│  User Input │
│  "MyPass123"│
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│  Generate Key   │
│  key = 'A'      │
└──────┬──────────┘
       │
       ▼
┌─────────────────────┐
│  XOR Encryption     │
│  'M'^'A' = result   │
│  'y'^'A' = result   │
│  ... for each char  │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Base64 Encode      │  (React only)
│  btoa(encrypted)    │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Save to Storage    │
│  + title            │
│  + user             │
│  + encrypted        │
│  + key              │
│  + strength         │
│  + timestamp        │
│  + expiry           │
└─────────────────────┘
```

### Password Retrieve Process
```
┌─────────────────────┐
│  Load from Storage  │
│  Get encrypted data │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Base64 Decode      │  (React only)
│  atob(encoded)      │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  XOR Decryption     │
│  encrypted ^ key    │
│  = original pass    │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Display to User    │
│  "MyPass123"        │
└─────────────────────┘
```

---

## 🔄 State Management (React)

### Zustand Store Flow
```
┌──────────────────────────────────────────────┐
│              Component                        │
│  const passList = usePasswordStore(          │
│    state => state.passList                   │
│  );                                          │
└────────────────┬─────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────┐
│         Zustand Store                         │
│  ┌────────────────────────────────────┐      │
│  │         State                      │      │
│  │  - passList: []                    │      │
│  │  - isAuthenticated: false          │      │
│  │  - searchQuery: ""                 │      │
│  │  - selectedPass: null              │      │
│  └────────────────────────────────────┘      │
│  ┌────────────────────────────────────┐      │
│  │         Actions                    │      │
│  │  - addNewPass()                    │──┐   │
│  │  - updatePass()                    │  │   │
│  │  - deletePass()                    │  │   │
│  │  - loadPasswords()                 │  │   │
│  │  - setSearchQuery()                │  │   │
│  └────────────────────────────────────┘  │   │
└──────────────────────────────────────────┼───┘
                                           │
                                           ▼
                                  ┌────────────────┐
                                  │  LocalStorage  │
                                  │  - Save/Load   │
                                  └────────────────┘
```

---

## 📊 Data Structure

### C++ Password Structure
```cpp
struct pass {
    string title;        // "Gmail"
    string userinfo;     // "user@gmail.com"
    string hashed;       // Hash for verification
    string encrypted;    // XOR encrypted password
    char key;            // 'A'
    int strength;        // 6
    string timestamp;    // "Sat Nov 16 10:30:45 2025"
    string expiry_date;  // "Thu Feb 14 10:30:45 2026"
}

// Stored in priority_queue<pass> sorted by strength
```

### React Password Object
```javascript
{
    id: "1700123456789",
    title: "Gmail",
    user: "user@gmail.com",
    encrypted: "aGVsbG8=",  // Base64 encoded
    key: "MyKey123",
    level: 6,
    strengthText: "Good",
    timestamp: "2025-11-16T10:30:45.123Z",
    expiry: "2026-02-14T10:30:45.123Z",
    status: "Valid"
}

// Stored in array: passwordList[]
```

---

## 🎯 Component Hierarchy (React)

```
App.jsx
├── Router
│   ├── Login.jsx
│   │   ├── Input.jsx
│   │   └── Button.jsx
│   │
│   └── Authenticated Layout
│       ├── Navbar.jsx
│       │
│       └── Routes
│           ├── Dashboard.jsx
│           │   └── Button.jsx
│           │
│           ├── AllPasswords.jsx
│           │   ├── SearchBar.jsx
│           │   ├── PasswordCard.jsx
│           │   │   └── Button.jsx
│           │   └── Modal.jsx
│           │       └── Button.jsx
│           │
│           ├── AddPassword.jsx
│           │   ├── Input.jsx
│           │   └── Button.jsx
│           │
│           ├── EditPassword.jsx
│           │   ├── Input.jsx
│           │   └── Button.jsx
│           │
│           └── Settings.jsx
│               ├── Button.jsx
│               └── Modal.jsx
│                   └── Button.jsx
```

---

## 🔄 User Flow Diagrams

### First Time User (React)
```
Start
  │
  ▼
Login Page
  │
  ├─ No master password?
  │    │
  │    ▼
  │  Create Master Password
  │    │
  │    ▼
  │  Confirm Password
  │    │
  │    ▼
  └────┬─────────────┐
       │             │
       ▼             ▼
   Dashboard    Store in LocalStorage
       │
       ▼
   Add First Password
       │
       ▼
   View in All Passwords
```

### Returning User (React)
```
Start
  │
  ▼
Login Page
  │
  ├─ Has master password?
  │    │
  │    ▼
  │  Enter Password
  │    │
  │    ├─ Correct? ──Yes──┐
  │    │                  │
  │    └─ Wrong? ──Retry (3x)
  │                        │
  └────────────────────────┘
                           │
                           ▼
                      Dashboard
                           │
           ┌───────────────┼───────────────┐
           │               │               │
           ▼               ▼               ▼
    Add Password    View Passwords    Settings
```

---

## 📁 File Storage (C++)

### File Structure
```
Project Folder/
├── password.cpp
├── password.exe
├── master.txt              ← Hashed master password
├── passwords.txt           ← Encrypted passwords
└── exported_passwords.txt  ← Export file (optional)
```

### passwords.txt Format
```
Gmail|user@gmail.com|encrypted_string|A|6|hashed|Sat Nov 16...|Thu Feb 14...
Facebook|user@fb.com|encrypted_string|B|5|hashed|Sat Nov 16...|Thu Feb 14...
```

Each field separated by `|`:
1. Title
2. Username
3. Encrypted password
4. Encryption key
5. Strength level
6. Hashed password
7. Timestamp
8. Expiry date

---

## 🌈 UI Component Tree (React)

### PasswordCard Component
```
PasswordCard
├── Card Container (glass effect)
│   ├── Header Section
│   │   ├── Title (h3)
│   │   ├── Username (p)
│   │   └── Status Badge (colored)
│   │
│   ├── Password Display Section
│   │   ├── Lock Icon
│   │   ├── Password Text (hidden/shown)
│   │   ├── Toggle Button (eye icon)
│   │   └── Copy Button (copy icon)
│   │
│   ├── Strength Indicator Section
│   │   ├── Label (text)
│   │   ├── Score (text)
│   │   └── Progress Bar (colored)
│   │
│   └── Footer Section
│       ├── Timestamp (text)
│       └── Action Buttons
│           ├── Edit Button
│           └── Delete Button
```

---

## 🎨 Style Architecture (React)

### Tailwind Configuration
```
tailwind.config.js
├── Colors
│   ├── dark-bg: '#0a0a0f'
│   ├── dark-card: '#141419'
│   ├── dark-border: '#1f1f26'
│   ├── dark-text: '#e4e4e7'
│   ├── dark-muted: '#a1a1aa'
│   └── primary: '#6366f1'
│
├── Theme Extensions
│   ├── Backdrop Blur
│   └── Custom Utilities
│
└── Plugins
    └── (none currently)

index.css
├── Base Styles
│   ├── Body styles
│   └── Border colors
│
├── Custom Utilities
│   ├── .scrollbar-hide
│   ├── .glass (glassmorphism)
│   └── .card-hover
│
└── Animations
    └── Framer Motion
```

---

## 🔐 Security Layers

### Multi-Layer Security
```
┌─────────────────────────────────────────┐
│         User Interface Layer            │
│  - Masked input                         │
│  - Hidden passwords by default          │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│      Authentication Layer               │
│  - Master password (hashed)             │
│  - 3 login attempts limit               │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│      Encryption Layer                   │
│  - XOR encryption per password          │
│  - Unique keys                          │
│  - Base64 encoding (React)              │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│      Storage Layer                      │
│  - File-based (C++)                     │
│  - LocalStorage (React)                 │
└─────────────────────────────────────────┘
```

---

## 📈 Password Lifecycle

### From Creation to Deletion
```
┌──────────────┐
│   CREATE     │
│  User adds   │
│  password    │
└──────┬───────┘
       │
       ▼
┌──────────────────┐
│  ENCRYPTION      │
│  XOR + Key       │
│  Calculate STR   │
│  Set expiry      │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│  STORAGE         │
│  Save to         │
│  storage         │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│  ACTIVE USE      │
│  ├─ View         │
│  ├─ Decrypt      │
│  ├─ Copy         │
│  └─ Search       │
└──────┬───────────┘
       │
       ├─────────────────┐
       │                 │
       ▼                 ▼
┌──────────────┐   ┌──────────────┐
│  UPDATE      │   │  EXPIRY      │
│  New pass    │   │  Status      │
│  Re-encrypt  │   │  changes     │
│  Reset exp   │   │  over time   │
└──────┬───────┘   └──────┬───────┘
       │                  │
       └──────┬───────────┘
              │
              ▼
       ┌──────────────┐
       │  DELETE      │
       │  Remove      │
       │  from        │
       │  storage     │
       └──────────────┘
```

---

## 🚀 Performance Optimization

### React Rendering Strategy
```
┌─────────────────────────────────────────┐
│  Component Level                        │
│  ├─ Zustand (minimal re-renders)       │
│  ├─ memo() for expensive components     │
│  └─ Lazy loading (future)              │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│  State Level                            │
│  ├─ Selective state subscription        │
│  ├─ Computed values (getSorted)         │
│  └─ Debounced search                    │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│  Storage Level                          │
│  ├─ Batch localStorage operations       │
│  ├─ Cache frequently accessed data      │
│  └─ JSON serialization optimization     │
└─────────────────────────────────────────┘
```

---

**Understanding these diagrams will help you:**
- 📚 Learn the architecture
- 🔧 Debug issues faster
- 🎨 Customize features
- 🚀 Extend functionality
- 🎓 Explain to others

**Happy Learning! 🎓**
